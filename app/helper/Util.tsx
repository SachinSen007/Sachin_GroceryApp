import {getAsyncData} from './AsyncStorageUtil';
import constant from '../config/Constant';

const printConsoleLog = (value: string) => {
  console.log(value);
};


const Util = {
  printConsoleLog
};

export default Util;
